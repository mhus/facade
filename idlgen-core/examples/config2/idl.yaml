
definitions: 
- definition.yaml

imports:

properties:
- package: com.example.idl
- name: Example

elements:
- struct: account
  fields:
    firstname: text
    lastname: text
    id: uuid
    street:
      type: text
      sequence: array
      validator:
       minSize: 1
       maxSize: 30
       mandatory: false
    other:
      type: text
      sequence: map

- service: account
  parameters:
  - name: id 
    type: uuid
  result: account

- service: accounts
  parameters:
  result: 
    type: account
    sequence: array
